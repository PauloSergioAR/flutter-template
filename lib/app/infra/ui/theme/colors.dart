import 'package:flutter/material.dart';

class UiColors {
  static const Color primary = Color(0xFF7900fe);
  static const Color secondary = Color(0xFFfa8608);
  static const Color green = Color(0xFF28C76F);
  static const Color red = Color(0xFFEA5455);
  static const Color orange = Color(0xFFFF9F43);
  static const Color _grey = Color(0xFF4B465C);

  static const Color fontColor = Color(0xFF0f031d);

  static Color primary100 = Color.lerp(primary, Colors.white, .8)!;
  static Color primary200 = Color.lerp(primary, Colors.white, .6)!;
  static Color primary300 = Color.lerp(primary, Colors.white, .4)!;
  static Color primary400 = Color.lerp(primary, Colors.white, .2)!;
  static Color primary600 = Color.lerp(primary, Colors.black, .10)!;
  static Color primary700 = Color.lerp(primary, Colors.black, .15)!;
  static Color primary800 = Color.lerp(primary, Colors.black, .20)!;
  static Color primary900 = Color.lerp(primary, Colors.black, .25)!;

  static Color secondary100 = Color.lerp(secondary, Colors.white, .8)!;
  static Color secondary200 = Color.lerp(secondary, Colors.white, .6)!;
  static Color secondary300 = Color.lerp(secondary, Colors.white, .4)!;
  static Color secondary400 = Color.lerp(secondary, Colors.white, .2)!;
  static Color secondary600 = Color.lerp(secondary, Colors.black, .10)!;
  static Color secondary700 = Color.lerp(secondary, Colors.black, .15)!;
  static Color secondary800 = Color.lerp(secondary, Colors.black, .20)!;
  static Color secondary900 = Color.lerp(secondary, Colors.black, .25)!;

  static Color green100 = Color.lerp(green, Colors.white, .8)!;
  static Color green200 = Color.lerp(green, Colors.white, .6)!;
  static Color green300 = Color.lerp(green, Colors.white, .4)!;
  static Color green400 = Color.lerp(green, Colors.white, .2)!;
  static Color green600 = Color.lerp(green, Colors.black, .10)!;
  static Color green700 = Color.lerp(green, Colors.black, .15)!;
  static Color green800 = Color.lerp(green, Colors.black, .20)!;
  static Color green900 = Color.lerp(green, Colors.black, .25)!;

  static Color red100 = Color.lerp(red, Colors.white, .8)!;
  static Color red200 = Color.lerp(red, Colors.white, .6)!;
  static Color red300 = Color.lerp(red, Colors.white, .4)!;
  static Color red400 = Color.lerp(red, Colors.white, .2)!;
  static Color red600 = Color.lerp(red, Colors.black, .10)!;
  static Color red700 = Color.lerp(red, Colors.black, .15)!;
  static Color red800 = Color.lerp(red, Colors.black, .20)!;
  static Color red900 = Color.lerp(red, Colors.black, .25)!;

  static Color orange100 = Color.lerp(orange, Colors.white, .8)!;
  static Color orange200 = Color.lerp(orange, Colors.white, .6)!;
  static Color orange300 = Color.lerp(orange, Colors.white, .4)!;
  static Color orange400 = Color.lerp(orange, Colors.white, .2)!;
  static Color orange600 = Color.lerp(orange, Colors.black, .10)!;
  static Color orange700 = Color.lerp(orange, Colors.black, .15)!;
  static Color orange800 = Color.lerp(orange, Colors.black, .20)!;
  static Color orange900 = Color.lerp(orange, Colors.black, .25)!;

  static Color grey100 = Color.lerp(_grey, Colors.white, .95)!;
  static Color grey200 = Color.lerp(_grey, Colors.white, .8)!;
  static Color grey300 = Color.lerp(_grey, Colors.white, .7)!;
  static Color grey400 = Color.lerp(_grey, Colors.white, .6)!;
  static Color grey = Color.lerp(_grey, Colors.white, .5)!;
  static Color grey600 = Color.lerp(_grey, Colors.white, .40)!;
  static Color grey700 = Color.lerp(_grey, Colors.white, .30)!;
  static Color grey800 = Color.lerp(_grey, Colors.white, .20)!;
  static Color grey900 = Color.lerp(_grey, Colors.white, .10)!;
}
